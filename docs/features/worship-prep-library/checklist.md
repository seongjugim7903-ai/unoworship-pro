# 준비찬양 라이브러리 · 조 흐름 · 연주용 보기 — 체크리스트

배경과 결정은 [context-notes.md](./context-notes.md) 참조.

## 1단계 — 라이브러리 (곡이 악보를 달고 다니게)  ✅ 코드 완료 · 마이그레이션 적용 대기

- [x] 곡 필드 3개 추가 — `tempo_bpm` · `time_signature` · `sung_key` (기존 `song_key` 는 악보 키)
      → 검증: 입력·저장·검색 결과 표시까지 세 값이 살아 있다
- [x] 마이그레이션 `worship_song_library` 테이블 — `church_id, team, title, song_key, sung_key, tempo_bpm, time_signature, arrangement, arrangement_custom, sheet_bucket, sheet_path, sheet_content_type, last_used_at`
      → 검증: `(church_id, team, title)` 고유 제약이 걸린다
- [x] `lib/worship-prep/songLibrary.ts` — 조회·등록·수정·삭제 (서버 전용)
      → 검증: 같은 제목 재등록이 행을 늘리지 않고 갱신한다
- [x] `app/api/worship-songs/route.ts` — GET(검색) · POST(등록·수정) · DELETE
      → 검증: 다른 교회 곡이 검색에 안 나온다
- [x] 준비찬양 저장 시 새 제목이면 라이브러리에 자동 등록·갱신
      → 검증: 저장 후 라이브러리 검색에 그 곡이 악보와 함께 나온다
- [x] **검색 결과 클릭 → 곡 행 채우기** (제목·조·구성·**악보**)
      → 검증: 악보 재업로드 없이 저장되고, 저장된 행의 `sheet_path` 가 원본과 같다
- [x] 기존 악보 재사용 저장 경로 — 지금 POST 는 새 파일 업로드만 상정한다
      → 검증: 파일 없이 `sheetPath` 만 넘겨도 저장된다
- [x] 라이브러리 곡 수정·삭제 UI
      → 검증: 삭제해도 `worship_prep_songs` 회차 기록은 남는다
- [x] **악보 삭제 버그 수정** — 회차 재저장이 라이브러리 악보를 지우던 문제
      → 검증: 라이브러리 소유·참조 중·이번에 재사용하는 경로는 정리 대상에서 빠진다
- [x] 기존 회차 기록을 라이브러리로 옮겨 심기 (마이그레이션)
      → 검증: 적용 후 검색에 쓰던 곡이 그대로 나온다
- [ ] **Supabase 마이그레이션 적용** `202608020002_worship_song_library.sql`
      → 검증: 배포 후 곡 저장 → 검색 → 클릭으로 채워지고 악보가 딸려온다

## 2단계 — 조 흐름  ✅ 완료

- [x] `lib/worship-prep/songKey.ts` — 조 표기 정규화 + 인접 관계 판정 (순수 함수). **부르는 키 기준**
      → 검증: `tests/song-key.test.ts` — `G`·`Am`·`G#`·`Bb`·`Em` 파싱, 못 알아듣는 표기는 `null`
- [x] 곡 목록에 조 흐름 표시 (`G → D → Em`) + 관계 라벨
      → 검증: 조가 비었거나 파싱 실패한 곡은 아무 것도 안 그린다
      → 화면 확인: `C → 5도 위 → G → 나란한조 → Em`
- [x] 부르는 조가 비면 악보 조로 본다 — 전조 없이 그대로 치는 경우가 흔하다

## 3단계 — 연주용 악보 보기

- [ ] 오늘 셋의 악보를 순서대로 넘기는 전체화면 보기 — **흰 배경 고정**
      → 검증: 곡 3개 셋에서 좌우 이동으로 끝까지 간다
- [ ] 넘김을 키 이벤트(`←` `→` `PageUp` `PageDown`)로 받는다 — 블루투스 발 페달이 그대로 동작한다
      → 검증: 키보드 방향키로 넘어간다
- [ ] 화면 꺼짐 방지 (Wake Lock)
      → 검증: 화면을 켜둔 채 방치해도 잠기지 않는다
- [ ] 셋 악보를 화면 진입 시 **한 번에** 미리 받기
      → 검증: 네트워크를 끊어도 이미 연 셋은 계속 넘어간다
- [ ] 모바일·태블릿에서 읽히는 크기
      → 검증: 실제 기기 폭에서 악보 글자가 읽힌다

## 마무리

- [ ] `npm test` 통과
- [ ] `npx tsc --noEmit` 통과
- [ ] 커밋 — 단계별로 나눈다 (라이브러리 / 조 흐름 / 연주용 보기)


## 4단계 — 디지털 악보 표준 기능  ✅ 코드 완료 · 마이그레이션 적용 대기

forScore·MobileSheets·Piascore 가 공통으로 갖춘 것 중 값이 큰 순서로 넣었다.

- [x] **여러 페이지 악보** — 곡당 한 장이던 것을 `sheet_pages` jsonb 배열로. 실제 악보는 2~4페이지다
      → 검증: 여러 장 선택 후 저장 → 연주 화면에서 장 단위로 넘어간다
- [x] **여백 잘라내기** — 올릴 때 흰 여백을 재서 crop 을 넣는다. 사람이 손으로 자르게 하지 않는다
      → 검증: `tests/sheet-pages.test.ts` 13개. 애매하면 자르지 않는다(MAX_TRIM·MIN_INK)
- [x] **가로 2페이지 펼침** — 세로 악보 두 장이 나란히. 넘김이 절반으로 준다
- [x] **메트로놈** — 저장된 BPM 을 그대로 쓴다. `M` 또는 ♩ 버튼
- [x] 곡 단위 이동은 숫자키·목록이, 장 단위 이동은 방향키·페달이 맡는다
- [ ] **Supabase 마이그레이션 적용** `202608020003_worship_sheet_pages.sql`

### 5단계 후보 (아직 안 함)

필기 주석 · 반 페이지 넘김 · 핀치 확대 · 자동 스크롤 · D.S./Coda 점프
