// 담당자 화면 — 화면 본체는 features/membership 에 있다.
//   맡은 팀에 팀원을 부르는 초대 링크를 복사한다

import LeaderHomePanel from '../../features/membership/LeaderHomePanel';

export default function Page() {
  return <LeaderHomePanel />;
}
